# Changelog

All notable changes to `@themusicdev/jamendo-ts-client` are documented here.
Generated by [git-cliff](https://git-cliff.org/) from conventional commits.

## [0.1.1] - 2026-08-23

### 🐛 Bug Fixes

- *(schemas)* Four real API-drift bugs found via live smoke-testing
- *(schemas)* Address PR-Agent review notes on autocomplete/feeds

### 📚 Documentation

- *(pre-publish)* Note Allowed Actions checkbox choice on npmjs
- Consolidate publish docs, retire client-plan.md

### 🧪 Testing

- *(integration)* Fix two tests passing API-rejected params

### ⚙️ Miscellaneous Tasks

- *(demo)* Add minimal Bun app exercising the published client
- Split integration tests into their own run script
- *(examples)* Rename demo/ to examples/, import directly from dist
- Build dist before typecheck and audit jobs
## [0.1.0] - 2026-08-23

### 🚀 Features

- Add library entry export and bun test placeholder
- *(core)* Add envelope parser and error types
- *(schemas)* Add track schemas and results validation
- *(core)* Add config resolution and ofetch-based fetcher
- *(core)* Add keyv cache wrapper with stable keys
- *(core)* Add rate-limit retry and throttle
- *(core)* Add request orchestrator and client facade
- *(tracks)* Add tracks endpoints with zod-typed params
- Add public exports, version 0.1.0, and README
- *(albums)* Add albums resource + shared LocalizedText schema
- *(artists)* Add artists resource
- *(playlists)* Add playlists resource
- *(radios)* Add radios resource
- *(reviews)* Add reviews resource
- *(misc)* Add feeds + autocomplete resources
- *(client)* Wire all resources into client + public exports + README
- *(core)* Add JamendoHttpError + redirect fetcher for 302 file endpoints
- *(client)* Wire requestRedirect into client + public exports
- *(radios)* Add stream endpoint with RadioStream schema

### 🐛 Bug Fixes

- Build dist before pack via prepack lifecycle
- *(tests)* Correct cache helper param typing
- *(core)* Isolate cache keys by client identity and harden throttle
- *(core)* Reject NaN maxConcurrent instead of silently disabling throttle
- *(core)* Overload request so non-array R requires resultsSchema

### 🚜 Refactor

- *(core)* Generalize results shape for non-array endpoints

### 📚 Documentation

- Add pre-publish checklist + release trigger instructions
- Drop dead release-notes grouping config (option A)
- Add PR-Agent setup guide with lessons + operational reference

### 🧪 Testing

- *(core)* Remove non-null assertions in fetcher tests
- Add live integration smoke test gated by JAMENDO_CLIENT_ID
- Integration smoke for 7 resources (C1)
- *(autocomplete)* Drop catalog-dependent bucket assertion
- *(radios)* Add stream endpoint tests

### ⚙️ Miscellaneous Tasks

- Add biome, lefthook, commitlint and typescript dev tooling
- Add tsc build pipeline and npm publish metadata
- Add keyv, ofetch, p-retry, zod runtime deps
- Track openapi spec, env template, and build plan
- Npm publish prep for 0.1.0 (D1)
- Add parallel check workflow
- Add PR-Agent automated review on OpenRouter free tier
- Harden workflows (pin actions, bun version, timeouts)
- Simplify PR-Agent to openrouter/free only
- Fix audit base detection, make dead-code/health advisory
- Move dead-code/health continue-on-error to step level
- Added worktrees to gitigmore
- *(schemas)* Drop dead schema + type exports
- *(pkg)* Scope package to @themusicdev + add npm metadata
- Add release workflow + npm pack assertion job
- Switch release.yml to npm Trusted Publishing (OIDC, no token)
- Create GitHub Release on tag + document 3-phase release flow
- *(pr-agent)* Use haiku-4.5 primary, free fallback
- *(pr-agent)* Cap max_tokens to fit credit budget
- *(pr-agent)* Cap openrouter output tokens, declare haiku context
- *(pr-agent)* Free-first ladder, paid last resort
- *(pr-agent)* Add openrouter/ prefix to glm-5.2 model id
- *(pr-agent)* Route describe tier to ollama cloud (separate provider)
- Remove dead release-notes grouping config
- *(pr-agent)* Disable auto /improve, keep /review + /describe
- *(pr-agent)* Use lighter gpt-oss:20b-cloud for ollama describe tier
